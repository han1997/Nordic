<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Debug-only, synthetic state: no account access, network playback or progress reports. -->
        <activity
            android:name=".VideoPlayerPreviewActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name="com.nordic.mediahub.UiCatalogActivity"
            android:exported="true"
            android:label="Nordic UI 样板"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|fontScale" />
    </application>
</manifest>
