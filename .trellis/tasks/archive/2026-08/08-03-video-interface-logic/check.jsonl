{"file": ".trellis/spec/backend/index.md", "reason": "Backend spec index — verification section defines the exact Gradle task sequence (compileDebugKotlin / testDebugUnitTest / lintDebug) the check sub-agent must run, and Pre-Development Checklist points to quality + Emby contract specs for review."}
{"file": ".trellis/spec/backend/quality-guidelines.md", "reason": "Check sub-agent verifies shared UI state components, anti-patterns (nested cards, shadow-at-rest, bounce easing), and test coverage expectations against the new search-expanded state, episode filter, player controls-visible default, and resume/restart action resolution."}
{"file": ".trellis/spec/backend/emby-integration.md", "reason": "Check sub-agent must verify: library selector + spotlight ordering still matches the Video browsing UI contract; resume/restart primary action stays consistent with Direct playback start position (resume < duration, played==true starts at 0); series detail episode rows still sort by season/episode and the Series streamUrl==null play gating is preserved; search/filter composition behavior unchanged."}
