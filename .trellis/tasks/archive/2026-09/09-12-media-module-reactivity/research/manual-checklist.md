# 0.1.8 模块开关真机验收清单（本轮未执行）

## 设备与执行边界

- 只读检查发现 PKJ110 在线，已安装版本为 0.1.7 / 7，设备当时正在使用其他应用。
- 为避免打断用户，本轮没有安装、启动或切换应用，没有点击开关、停止播放、卸载或清除数据。
- 自动化已覆盖配置 Flow 与设置/导航数据推导；以下真实 Compose 交互不据此标为通过。

## 空闲时复测

1. 记录原始模块组合，在确认没有正在播放的媒体后覆盖安装 `nordic-0.1.8.apk`，不要卸载或清数据。
2. [ ] 打开「设置 → 模块显示」，关闭视频：开关当场关闭，页面不退出；返回设置首页后视频专属分类/搜索项消失，通用设置仍在。
3. [ ] 返回媒体首页，视频导航消失；再次打开模块页重新显示视频，无需重启便出现，且不自动切页或播放。
4. [ ] 同一页面连续开/关多次，开关与其他入口每次同步，而非仅首次有效。
5. [ ] 依次覆盖七种有效组合；单模块时没有媒体导航/空把手，正在播放条独立保留；尝试关闭最后一个模块应拒绝并保持可见。
6. [ ] 从当前媒体进入设置后隐藏该媒体，设置页保持打开；退出设置后按音乐→有声书→视频回退到仍可见媒体。
7. [ ] 当前模块播放/暂停/准备中隐藏时先提示确认；取消不改变可见性，确认仅停止目标模块，不误停其他域；失败时保持模块可见。
8. [ ] 重启应用后组合与即时切换结果一致；必要时验证恢复默认同时刷新所有模块。
9. [ ] 测试结束恢复原始模块组合，不修改服务器、凭证、历史或下载。

APK 身份、签名、哈希和自动测试统计见同目录 `build-results.json`；真机实际执行后单独补日期与结果。
