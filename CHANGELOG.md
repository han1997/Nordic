# 更新日志

## [未发布]

### 新增
- 新增底部“配置”tab，集中管理 Navidrome、AudiobookShelf 和 Emby 连接信息，并支持保存前轻量测试连接。

### 改进
- 音乐播放页移除快进快退按钮（后退 10 秒 / 前进 30 秒），简化控制行为随机/上一首/播放暂停/下一首/循环五个按钮。
- 统一顶部动作、返回、音乐搜索和底部 Dock 的按钮图标，使用矢量图标替代文本符号，并为持久控制区增加轻量毛玻璃高光。
- 有声书播放器、音乐队列和专辑/歌单占位图标继续去除文本符号伪图标，统一为 Material 矢量图标并补齐关键操作无障碍描述。
- 音乐、有声书、视频页面移除旧的右上角配置面板，空状态改为引导用户前往配置 tab。
- 音乐模块继续补齐歌单创建/重命名/删除入口、歌曲页本地筛选、歌词状态提示和队列当前播放定位，让大曲库浏览与播放管理更顺手。
- 音乐搜索框新增一键清除入口，清空关键词时会立即取消待执行搜索并恢复搜索落地页，避免旧结果或加载状态残留。
- 有声书播放器在暂未取得总时长时，`+30` / `-30` 相对跳转会继续基于当前播放位置安全前进或回退，不再被错误归零。
- 音乐播放页细线进度条拖动改为按手指绝对位置稳定跟随，取消拖动不再误触发 seek，并新增 10 秒后退 / 30 秒前进控制。
- 视频配置页明确当前版本仅支持 Emby，Plex/WebDAV 作为后续计划展示但不可误操作为已接入服务；同步 README/PRODUCT 中的视频能力说明。
- 共享加载状态卡片新增轻量进度线，让同步、搜索和详情加载过程更明确地呈现为进行中。
- 音乐播放队列的上移、下移和拖动排序现在会真实更新播放队列，避免队列编辑控件只有视觉反馈。
- 视频浏览页将电视剧作为整体展示，首页网格、搜索结果和非“继续观看”推荐区不再混入单集条目；单集仍保留在继续观看和电视剧详情页分集列表中。
- 视频播放页改为沉浸式控制层，新增播放信息面板，可在播放中查看标题、类型、年份、时长、进度、评分和简介。
- 底部导航 Dock 不再在滚动或惯性滑动停止后自动重新出现，改为隐藏时显示低干扰底部小把手，点击后恢复完整 Dock；切换 tab 或关闭播放器仍会自动恢复导航，避免阅读内容时被自动出现的 Dock 打断。
- 音乐播放页改为更贴近主流音乐 App 的布局：控件全部替换为矢量图标，进度条改为细线+小圆 thumb，控制行重排为随机/上一首/播放暂停/下一首/循环（单曲循环显示 “1” 角标）；标题与艺人移到封面下方，顶栏瘦身为下拉箭头 + “正在播放”；新增 ♥ 收藏按钮（star/unstar，乐观更新），新增下滑手势关闭播放页，加深封面背景毛玻璃质感。
- 音乐详情页返回会尽量回到进入前的来源页；刷新专辑、歌手或歌单列表后会同步修正已打开详情，配置切换导致回到音乐首页时会显示轻量说明。
- 有声书和视频在配置切换导致详情、搜索或筛选状态被重置时会显示轻量说明；视频切换媒体库后会同步校正类型筛选，避免旧筛选残留到新媒体库。
- 音乐播放页下滑关闭手势改为随手指渐进跟随：拖动过程中封面与内容整体下移、轻微缩小并淡化，越过屏幕高度四分之一阈值才平滑收起关闭，未达阈值则以无弹跳的物理回弹恢复原位；不再出现微小滑动即触发关闭、无视觉反馈的生硬跳变。
- 音乐播放页♥收藏失败时，会在顶部短暂浮现一条 pill 提示“收藏操作失败，已恢复”，2 秒后自动消失，避免乐观更新失败回退时用户只看到爱心“自己跳回去”却不知发生何事。
- 音乐播放页单曲循环 “1” 角标调整为低对比的轻量小圆 chip，去掉原本抢眼的纯白底块，改为微透 surface 底色加细 primary 描边并略外凸 2dp，让角标与循环图标更协调、不喧宾夺主。
- 音乐播放队列 Sheet 交互打磨：首次打开静默定位当前播放，切歌后改为平滑滚动对齐，仅在当前项已离开视口时触发避免多余动画；拖动重排去掉原先的阴影反馈（违反 Flat-at-Rest），改为微幅抬起态（scaleY/alpha 轻变）并保留跟手平移，松手以 tween 平滑归位；删除行带 fadeOut + shrinkVertically 退场动画，避免 LazyColumn 默认 diff 硬跳。
- 音乐库浏览视觉层级统一：歌曲排序控件从 LazyRow pill 改为与专辑排序、首页 tab 一致的自绘分段样式（外容器 surfaceVariant 0.56 alpha + 16dp 圆角 + 48dp 高，选中 surface 0.96 + tonalElevation），三种分段控件视觉语言完全对齐。
- 专辑列表行和歌单列表行封面尺寸从 56dp 校正为 52dp、圆角从 16dp 校正为 12dp，与 DESIGN.md list row 规范（52dp@12dp）一致。
- 音乐各页加载态统一改用共享 `MediaLoadingCard`（含轻量进度线），替换专辑列表、歌手详情、专辑详情、歌单列表和歌单详情中原先的内联 `Box` + `Text` 占位，加载视觉与首页同步态完全一致。
- 音乐播放页歌词面板改为 `LazyColumn` 自动滚动：同步歌词随播放进度平滑滚动对齐当前行，首次加载静默定位避免开场动画；高亮行颜色与字重改为 `animateColorAsState` / `animateFloatAsState` 平滑过渡，字号固定统一不再用字号区分高亮，避免行布局抖动；非同步歌词保持仅显示前 N 行不滚动。
- 音乐播放队列拖动重排改为实时让位：拖动过程中被拖行跟手平移并保持微幅抬起态（scaleY/alpha 轻变，无阴影），其他行根据被拖行位置实时动画位移让出空间，松手以 tween 平滑归位后提交排序变更；不依赖 Compose Foundation 1.7+ `animateItem()` API，与当前 BOM 2024.01.00 / 1.6.x 兼容。
- 音乐播放队列 Sheet 和均衡器 Sheet 启用两段式展开：先展开到中间态（部分可见，队列显示前几行与当前播放高亮，均衡器显示预设选择区域），上拉可完全展开；两个 Sheet 行为一致，保持 Sheet 间一致性。

### 开发规范
- 新增中文化沟通与文档更新规范，明确中文优先范围、技术原名保留边界，以及 `CHANGELOG.md` 和 `README.md` 的更新触发条件。
- 补充 PowerShell 中文显示乱码的误判防线：先用 `rg`、`git diff` 或编码测试确认源码是否真的损坏，再修复编码。

## [1.5.0] - 2026-06-12

### 新增
- 主题自动跟随系统日间/夜间模式
- Navidrome音乐服务器自动连接
  - 保存配置后自动加载音乐数据
  - 显示最近添加的专辑
  - 显示最近播放的歌曲
  - 显示歌手列表
- 音乐主页面全新布局（参考图片设计）：
  - 顶部标签：发现、歌单、搜索
  - 内容分类：最近添加、最近播放、歌手、专辑
  - 封面图片自动显示
  - 歌曲时长、专辑数量显示

### 技术改进
- 使用Retrofit连接Navidrome API
- 使用Coil加载封面图片
- 响应式数据加载

## [1.4.0] - 2026-06-12

### 新增
- 服务器配置持久化存储
  - 使用 DataStore 本地保存配置
  - 应用重启后自动加载已保存配置
  - 每个页面添加"保存配置"按钮
- 添加网络库依赖（Retrofit, OkHttp）为连接功能做准备

### 改进
- 配置界面优化：明确的保存操作
- 配置自动同步到UI

## [1.3.0] - 2026-06-11

### 新增
- 流畅的动画系统：
  - 列表项渐入动画（逐个出现，50ms 延迟）
  - 配置面板展开/收起动画
  - 标签页切换淡入淡出
  - 按钮按压弹性反馈
  - 卡片点击缩放效果
  - 视频服务器类型切换动画
  - 主题切换图标交互动画

### 改进
- 使用 Spring 物理动画引擎
- 快出慢入缓动函数（FastOutSlowInEasing）
- 所有交互都有即时反馈

## [1.2.0] - 2026-06-11

### 新增
- 日间/深色主题切换功能（点击右上角太阳/月亮图标）
- 完整的服务器配置支持：
  - Navidrome: 服务器地址、用户名、密码
  - AudiobookShelf: 服务器地址、用户名、密码
  - Emby: 服务器地址、用户名、密码、API Key（可选）
  - Plex: 服务器地址、用户名、密码
  - WebDAV: 服务器地址、用户名、密码
- 密码字段自动隐藏输入

### 改进
- 日间模式配色更柔和舒适
- 主题色自动适配当前模式

## [1.1.0] - 2026-06-11

### 新增
- 每个页面添加服务器配置功能
- 视频页面支持切换 Emby/Plex/WebDAV

### 改进
- 深色主题，紫色渐变设计
- 更醒目的标题字体和间距
- 渐变色卡片背景
- 优化视觉层次和对比度

## [1.0.0] - 2026-06-11

### 新增
- 初始版本
- 三标签界面：音乐、有声书、视频
- 底部导航栏
- 正在播放栏（占位）
- Jetpack Compose 实现
- 柔和的淡紫色主题设计
